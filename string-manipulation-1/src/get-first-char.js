/* exported getFirstChar */
/*
Use a funtion to retrieve the first character of a string
input: word (string) 'LearningFuze'
output: 'L'
recieve the word they want
create storage to put the first character of the word
get the first character of that word
give back that character

 */
function getFirstChar(string) {
  var firstChar = '';
  firstChar = string.charAt(0);
  return firstChar;
}
