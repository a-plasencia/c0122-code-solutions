/* exported getValue */
/*
Create a function that gets the value of what the object sets
input: (object, 'keyName')
output: 'value inside keyname'
-gives back the value inside of the object.
*/

function getValue(object, key) {
  return object[key];
}
